import SectionTitle from "./SectionTitle";

const Projects = () => {
  return (
    <div className="view-container mt-24">
      <SectionTitle badges={["design", "dev"]}>Selected Projects</SectionTitle>
    </div>
  );
};

export default Projects;
